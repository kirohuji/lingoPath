# LingoPath（言途学社）

后台管理系统 Monorepo：

- `apps/backend`: NestJS API + WebSocket + Prisma
- `apps/frontend`: React + Vite + shadcn/ui
- `packages/shared-types`: 前后端共享类型与错误码

## 启动

1. 安装依赖：`pnpm install`
2. 复制环境变量模板并填写
3. 启动：`pnpm dev`
